---
phase: 02-basic-insect-implementation
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - scripts/camera/auto_distance.gd
  - scenes/main.tscn
  - scripts/ui/camera_distance_display.gd
  - scenes/ui/debug_panel.tscn
autonomous: true
requirements:
  - CAM-04
user_setup: []

must_haves:
  truths:
    - "Camera automatically maintains reasonable viewing distance from insects"
    - "Camera distance adjusts based on insect size (0.1 m reference)"
    - "Distance adjustment is smooth interpolation, not snapping"
    - "Current camera distance is displayed in UI debug panel"
  artifacts:
    - path: "scripts/camera/auto_distance.gd"
      provides: "Camera distance adjustment logic"
      contains: "func calculate_optimal_distance(insect_size: float), func _process(delta)"
    - path: "scenes/main.tscn"
      provides: "Updated camera with auto‑distance script attached"
      contains: "Camera3D node with auto_distance.gd script"
    - path: "scripts/ui/camera_distance_display.gd"
      provides: "UI display of current camera distance"
      contains: "func update_distance_display(distance: float)"
    - path: "scenes/ui/debug_panel.tscn"
      provides: "Debug panel showing camera distance"
      contains: "Label for camera distance"
  key_links:
    - from: "scripts/camera/auto_distance.gd"
      to: "scenes/insects/insect_base.tscn"
      via: "distance calculation uses insect size"
      pattern: "optimal_distance = insect_size * 20.0"
    - from: "scripts/camera/auto_distance.gd"
      to: "scripts/ui/camera_distance_display.gd"
      via: "signal camera_distance_changed"
      pattern: "emit_signal('camera_distance_changed', current_distance)"
---

<objective>
Implement automatic camera distance maintenance based on insect size, with smooth interpolation and UI feedback.

Purpose: Fulfill CAM‑04 requirement: "Camera automatically maintains reasonable viewing distance from insects". Implement D-13 (automatic distance adjustment), D-14 (smooth interpolation), D-15 (based on insect size). Provide debug UI to verify distance behavior.

Output: Camera script that adjusts distance from insects, smooth interpolation, distance display in debug panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-basic-insect-implementation/02-CONTEXT.md
@.planning/phases/02-basic-insect-implementation/02-PATTERNS.md
@.planning/research/OBSERVATION_GAMEPLAY.md
@.planning/research/ARCHITECTURE.md

# Dependencies
@.planning/phases/02-basic-insect-implementation/02-04-PLAN.md
@.planning/phases/02-basic-insect-implementation/02-UI-SPEC.md

<interfaces>
<!-- From Phase 1 camera -->
From Phase 1 (assumed): Camera3D node in main scene with free‑fly controls.

From CONTEXT.md decisions:
- D-13: Camera distance maintenance: Automatic distance adjustment
- D-14: Adjustment logic: Smoothly interpolate to optimal distance
- D-15: Optimal distance calculation: Based on insect size
- D-11: Insect size: 0.1 m (10 cm)

From PATTERNS.md camera pattern (OBSERVATION_GAMEPLAY.md lines 170-190):
```gdscript
extends Camera3D
var follow_target: Node3D = null
var spring_arm: SpringArm3D

func _ready():
    spring_arm = $SpringArm3D

func _process(delta):
    if follow_target:
        global_position = global_position.lerp(follow_target.global_position, delta * 5.0)
```

From UI-SPEC.md:
- Camera distance indicator: Bottom-left corner
- Debug panel: Bottom-right panel
- Color accent: #4CAF50 for active controls
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create camera auto‑distance script</name>
<files>scripts/camera/auto_distance.gd</files>
  <action>
Create `scripts/camera/auto_distance.gd` extending `Camera3D`.

Logic:
1. Properties:
   - `var target_distance: float = 5.0` (optimal distance to maintain).
   - `var current_distance: float = 5.0` (actual distance).
   - `var interpolation_speed: float = 2.0` (lerp speed).
   - `var reference_insect_size: float = 0.1` (0.1 m).
2. Signal: `camera_distance_changed(new_distance: float)`.
3. Method `calculate_optimal_distance(insect_size: float) -> float`:
   - Base formula: `optimal = insect_size * 20.0` (2 m for 0.1 m insect).
   - Clamp between min/max (e.g., 1 m to 10 m).
4. In `_process(delta)`:
   - If insects present (need insect manager reference), calculate average insect size.
   - Update `target_distance` using `calculate_optimal_distance`.
   - Lerp `current_distance` toward `target_distance`.
   - Adjust camera position (move forward/back along view direction) to achieve `current_distance`.
   - Emit `camera_distance_changed` signal.

For now, assume insect manager accessible. Use dummy insect size 0.1 m.

Smooth interpolation per D-14: `current_distance = lerp(current_distance, target_distance, delta * interpolation_speed)`.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Script exists with distance calculation and smooth interpolation; signal emitted on change.</done>
</task>

<task type="auto">
  <name>Task 2: Attach auto‑distance script to main camera</name>
<files>scenes/main.tscn</files>
  <action>
1. Open main scene.
2. Find the camera node (created in Phase 1). It should have free‑fly camera script.
3. Attach `auto_distance.gd` script to camera (multiple scripts allowed).
4. Configure script properties:
   - `reference_insect_size = 0.1`
   - `interpolation_speed = 2.0`
5. Ensure camera distance adjustment doesn't conflict with free‑fly controls. Auto‑distance should modify camera position along its forward axis only, preserving user rotation.

Integration: Camera now has two behaviors: manual free‑fly (Phase 1) + auto‑distance (Phase 2). They should work together: user can manually move, auto‑distance gently adjusts distance.

Test: run scene, move camera close to insects; camera should slowly adjust distance.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Camera in main scene has auto‑distance script attached; properties set.</done>
</task>

<task type="auto">
  <name>Task 3: Create camera distance UI display and debug panel</name>
<files>scripts/ui/camera_distance_display.gd, scenes/ui/debug_panel.tscn</files>
  <action>
1. Create `scripts/ui/camera_distance_display.gd` extending `Control`.
   - Property `var distance_label: Label`.
   - Method `update_distance_display(distance: float)`: format as "X.XX m", update label.
   - Connect to camera's `camera_distance_changed` signal.

2. Open `debug_panel.tscn` (create if not exists per PATTERNS.md). Add `CameraDistanceDisplay` node.
   - Add Label child for distance.
   - Attach `camera_distance_display.gd`.
   - Position per UI‑SPEC: bottom‑left corner.

3. In main scene, connect camera signal to UI display.

4. Add debug controls to panel (optional):
   - Button to toggle auto‑distance on/off.
   - Slider to adjust interpolation speed.

Follow UI‑SPEC colors: use accent color #4CAF50 for active distance readout.

This provides visual verification of CAM‑04.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Debug panel shows camera distance; updates when camera moves; UI matches spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Camera distance calculation | Uses internal insect size only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Denial of Service | Camera oscillation | accept | Interpolation speed limited; no feedback loops. |
| T-02-10 | Spoofing | Insect size input | accept | Size from trusted stats resource, not external. |
</threat_model>

<verification>
- Run scene, move camera close to insects; observe camera slowly adjusts distance.
- Check debug panel displays distance changing smoothly.
- Verify distance calculation: for 0.1 m insect, target ≈ 2 m.
</verification>

<success_criteria>
1. Camera automatically adjusts distance based on insect size.
2. Distance adjustment uses smooth interpolation (no snapping).
3. Target distance scales with insect size (0.1 m → ~2 m).
4. Camera distance displayed in debug UI.
</success_criteria>

<output>
After completion, create `.planning/phases/02-basic-insect-implementation/02-05-SUMMARY.md`
</output>