---
phase: 02-basic-insect-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenes/insects/insect_base.tscn
  - scripts/insects/insect.gd
  - resources/insect_stats.tres
autonomous: true
requirements:
  - INSECT-01
  - INSECT-05
user_setup: []

must_haves:
  truths:
    - "Developer can instantiate an insect scene with primitive shape representation"
    - "Insect scene has a skeleton with 6-10 bones for animation"
    - "Insect species can be configured via stats resource (size, color, pattern parameters)"
    - "Three distinct primitive shapes exist (cricket, beetle, spider)"
  artifacts:
    - path: "scenes/insects/insect_base.tscn"
      provides: "Reusable insect scene with skeleton and primitive mesh"
      contains: "Skeleton3D node with 6-10 bones, MeshInstance3D with primitive shape"
    - path: "scripts/insects/insect.gd"
      provides: "Insect behavior script with stats reference"
      exports: "@export var stats: InsectStats"
    - path: "resources/insect_stats.tres"
      provides: "Resource holding insect species parameters"
      contains: "size: float, color: Color, pattern_complexity: int, species: String"
  key_links:
    - from: "scripts/insects/insect.gd"
      to: "resources/insect_stats.tres"
      via: "@export reference"
      pattern: "@export var stats: InsectStats"
    - from: "scenes/insects/insect_base.tscn"
      to: "scripts/insects/insect.gd"
      via: "Script attached to root node"
      pattern: "extends CharacterBody3D"
---

<objective>
Create the foundational insect scene with primitive shapes and skeleton, plus a stats resource to configure species parameters.

Purpose: Provide a reusable insect scene that can be instantiated for different species (cricket, beetle, spider) with distinct shapes. The skeleton enables future animations, and the stats resource drives visual distinctiveness (size, color, pattern).

Output: `insect_base.tscn`, `insect.gd`, `insect_stats.tres` ready for animation and spawning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-basic-insect-implementation/02-CONTEXT.md
@.planning/phases/02-basic-insect-implementation/02-PATTERNS.md
@.planning/phases/02-basic-insect-implementation/02-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/NATURAL_ENVIRONMENT.md
@.planning/research/OBSERVATION_GAMEPLAY.md

# Phase 1 output assumed
@.planning/phases/01-environment-camera-setup/01-CONTEXT.md

<interfaces>
<!-- Key patterns from research for insect scene creation -->

From ARCHITECTURE.md (Resource pattern):
```gdscript
# BattlerStats.gd (extends Resource)
@export var power: int = 10
@export var speed: int = 10
@export var defense: int = 10
@export var max_health: int = 100

# Battler.gd
@export var stats: BattlerStats
func _ready():
    max_health = stats.max_health
    current_health = max_health
```

From PATTERNS.md:
- Insect base scene: `scenes/insects/insect_base.tscn`
- Insect script: `scripts/insects/insect.gd`
- Resource pattern: `resources/insect_stats.tres`

From CONTEXT.md decisions:
- D-01: Primitive shapes with textures
- D-05: Skeletal animations
- D-06: 6–10 bones per insect
- D-09: Species: cricket, beetle, spider
- D-10: Distinct by size, color, pattern, shape
- D-11: Insect size: 0.1 m (10 cm) relative to 0.5 m terrain tile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create insect stats resource and script</name>
  <files>resources/insect_stats.tres, scripts/insects/insect.gd</files>
  <action>
1. Create `scripts/insects/insect.gd` extending `CharacterBody3D` (for future physics). Add:
   - `@export var stats: InsectStats`
   - `var species: String` set from stats
   - `func _ready():` apply stats.size to scale, store species
   - Signal declarations: `spawned`, `died`, `animation_state_changed`

2. Create `resources/insect_stats.gd` extending `Resource`. Define exported properties:
   - `species: String` ("cricket", "beetle", "spider")
   - `size: float = 0.1` (meters)
   - `color: Color = Color.WHITE`
   - `pattern_complexity: int = 5`
   - `walk_speed: float = 1.0`
   - `idle_animation_speed: float = 1.0`

3. Create `resources/insect_stats.tres` as an instance of the resource, set default values for cricket.

Follow resource pattern from ARCHITECTURE.md lines 145-156. Ensure the script is clean with no placeholder logic beyond applying stats.

Note: Primitive shape and skeleton will be added in Task 2.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Files exist with correct structure; `insect.gd` references `InsectStats` resource; `insect_stats.tres` can be edited in Godot inspector.</done>
</task>

<task type="auto">
  <name>Task 2: Create insect base scene with primitive shapes and skeleton</name>
  <files>scenes/insects/insect_base.tscn</files>
  <action>
1. Create `scenes/insects/insect_base.tscn` with root node `CharacterBody3D` (same as script).
2. Attach `insect.gd` script to root.
3. Add `MeshInstance3D` child with a primitive shape (start with `BoxMesh` for prototyping). Set size to (0.1, 0.1, 0.1) (10 cm cube).
4. Add `Skeleton3D` child with 6‑10 bones. Place bones roughly: root, head, thorax, abdomen, legs (2–4), antennae (optional). Use `BoneAttachment3D` nodes if needed.
5. Skin the mesh to skeleton (simple weight painting: assign each vertex to nearest bone). For prototyping, use automatic weighting.
6. Create three scene variants via inherited scenes? Instead, we'll rely on stats to differentiate; keep single base scene.

Goal: A functional skeleton that can be animated later. Use Godot's skeleton editor.

Reference D-06 (6–10 bones) and D-11 (size 0.1 m). Keep primitive shapes distinct per species later via script; for now generic cube.

7. Ensure the scene can be instantiated and appears in viewport.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Scene opens in Godot editor; skeleton has bones; mesh is skinned; root has `insect.gd` script attached.</done>
</task>

<task type="auto">
  <name>Task 3: Define three species stat resources</name>
  <files>resources/insect_stats.tres, resources/insect_stats_cricket.tres, resources/insect_stats_beetle.tres, resources/insect_stats_spider.tres</files>
  <action>
Create three distinct InsectStats resource instances for cricket, beetle, spider:

1. **Cricket**: size 0.1, color greenish (`Color("#4CAF50")`), pattern_complexity 6, walk_speed 1.2.
2. **Beetle**: size 0.12 (slightly larger), color dark brown (`Color("#5D4037")`), pattern_complexity 8, walk_speed 0.8.
3. **Spider**: size 0.08 (smaller), color black (`Color("#212121")`), pattern_complexity 4, walk_speed 1.5.

Save as separate `.tres` files: `insect_stats_cricket.tres`, `insect_stats_beetle.tres`, `insect_stats_spider.tres`.

Update `insect.gd` `_ready()` to apply color to mesh material (if material exists). For now, just store color; material will be added in Plan 02.

Ensure each resource has `species` string set appropriately.

This implements D-09 (species selection) and D-10 (visual distinction via size, color, pattern).
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Three resource files exist with distinct values; species string matches; color values differ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in this plan | All assets are local; no player input processed yet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | Resource loading | accept | Low risk; resource files are small. |
| T-02-02 | Tampering | Resource files | accept | Files are part of version control; no runtime modification. |
</threat_model>

<verification>
- Open Godot editor, load `insect_base.tscn`, confirm skeleton exists with 6‑10 bones.
- Select root node, check Inspector for `stats` property (can assign InsectStats resource).
- Load each species resource, verify distinct color and size values.
</verification>

<success_criteria>
1. Insect base scene exists with skeleton and primitive mesh.
2. Insect script references InsectStats resource.
3. Three species stat resources exist with distinct size, color, pattern parameters.
4. Scene can be instantiated in a test scene without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-basic-insect-implementation/02-01-SUMMARY.md`
</output>