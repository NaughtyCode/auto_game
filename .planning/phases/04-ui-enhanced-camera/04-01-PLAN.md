---
phase: 04-ui-enhanced-camera
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - res://ui/theme.tres
  - res://ui/panels/insect_info_panel.tscn
  - res://ui/panels/observation_hint_panel.tscn
  - res://ui/panels/camera_controls_panel.tscn
  - res://ui/panels/game_status_panel.tscn
  - res://ui/tracking_indicator.tscn
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - "UI panels use consistent colors per design spec"
    - "Insect info panel is positioned top-right with species label and track button"
    - "Observation hint panel is positioned bottom-right with hint label"
    - "Camera controls panel is positioned bottom-center with movement buttons"
    - "Game status panel is positioned top-left with time and observation labels"
    - "Tracking indicator is centered with crosshair visual"
  artifacts:
    - path: "res://ui/theme.tres"
      provides: "UI theme with colors, fonts, spacing"
      contains: "Dominant color #1E1E1E, Secondary #2D2D2D, Accent #4CAF50"
    - path: "res://ui/panels/insect_info_panel.tscn"
      provides: "Insect information panel UI"
      contains: "Control node hierarchy, Label for species, Label for status, Track Insect button"
    - path: "res://ui/panels/observation_hint_panel.tscn"
      provides: "Observation hint panel UI"
      contains: "Control node hierarchy, Label for hint text"
    - path: "res://ui/panels/camera_controls_panel.tscn"
      provides: "Camera controls panel UI"
      contains: "Buttons: Track Insect, Free Camera, Zoom In, Zoom Out, Pan Left, Pan Right, Rotate"
    - path: "res://ui/panels/game_status_panel.tscn"
      provides: "Game status panel UI"
      contains: "Label for time, Label for observation count"
    - path: "res://ui/tracking_indicator.tscn"
      provides: "Tracking indicator reticle"
      contains: "Center-screen Control node with crosshair or circle"
  key_links:
    - from: "res://ui/panels/insect_info_panel.tscn"
      to: "res://ui/theme.tres"
      via: "theme override"
      pattern: "theme_override"
    - from: "res://ui/panels/observation_hint_panel.tscn"
      to: "res://ui/theme.tres"
      via: "theme override"
      pattern: "theme_override"
---

<objective>
Create UI theme resource and base panel scenes for insect info, observation hints, camera controls, game status, and tracking indicator.

Purpose: Establish visual foundation for Phase 4 UI elements with consistent styling per UI-SPEC.md design contract.

Output: Theme resource and five UI scenes placed in appropriate directories, ready for wiring to game systems.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-enhanced-camera/04-UI-SPEC.md

**UI-SPEC Design Contract Summary:**
- Colors: Dominant #1E1E1E, Secondary #2D2D2D, Accent #4CAF50, Destructive #F44336
- Spacing scale: xs=4px, sm=8px, md=16px, lg=24px, xl=32px, 2xl=48px, 3xl=64px
- Typography: Label 14px regular, Body 16px regular, Heading 20px bold, Display 28px bold
- Copywriting: Panel titles, button labels per UI-SPEC
- Layout placement: Insect info top-right, observation hints bottom-right, camera controls bottom-center, game status top-left, tracking indicator center-screen
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI theme resource</name>
  <files>res://ui/theme.tres</files>
  <read_first>
    - @.planning/phases/04-ui-enhanced-camera/04-UI-SPEC.md (colors, spacing, typography)
    - Existing Godot project structure (if any) to know where to place theme
  </read_first>
  <action>
    1. Ensure directory `res://ui/` exists (create if missing).
    2. Create a new Theme resource in Godot Editor (or create manually as a .tres file).
    3. Define the following colors as theme constants:
       - `dominant_color`: #1E1E1E
       - `secondary_color`: #2D2D2D
       - `accent_color`: #4CAF50
       - `destructive_color`: #F44336
    4. Define spacing constants matching UI-SPEC scale (xs=4, sm=8, md=16, lg=24, xl=32, 2xl=48, 3xl=64) as integers.
    5. Define font sizes: `label_font_size=14`, `body_font_size=16`, `heading_font_size=20`, `display_font_size=28`.
    6. Save as `res://ui/theme.tres`.
  </action>
  <verify>
    <automated>test -f "res://ui/theme.tres" &amp;&amp; grep -q "dominant_color" "res://ui/theme.tres" &amp;&amp; echo "Theme exists with constants"</automated>
    - File `res://ui/theme.tres` exists and is a valid Godot Theme resource.
    - Theme contains color constants with exact hex values above.
    - Theme contains spacing constants with exact integer values.
    - Theme contains font size constants.
  </verify>
  <done>Theme resource created and ready to be referenced by UI panels.</done>
</task>

<task type="auto">
  <name>Task 2: Create base UI panel scenes</name>
  <files>
    res://ui/panels/insect_info_panel.tscn
    res://ui/panels/observation_hint_panel.tscn
    res://ui/panels/camera_controls_panel.tscn
    res://ui/panels/game_status_panel.tscn
    res://ui/tracking_indicator.tscn
  </files>
  <read_first>
    - @.planning/phases/04-ui-enhanced-camera/04-UI-SPEC.md (layout placement, copywriting, visual hierarchy)
    - res://ui/theme.tres (created in Task 1)
  </read_first>
  <action>
    1. Create directory `res://ui/panels/`.
    2. For each panel, create a new Control node scene.
    3. Apply theme from `res://ui/theme.tres` as theme override.
    4. Set panel size and position according to layout placement:
       - Insect info panel: Anchor top-right, margin right=lg, margin top=lg, width 300px, height 200px.
       - Observation hint panel: Anchor bottom-right, margin right=lg, margin bottom=lg, width 400px, height 100px.
       - Camera controls panel: Anchor bottom-center, margin bottom=lg, width 500px, height 80px.
       - Game status panel: Anchor top-left, margin left=lg, margin top=lg, width 200px, height 80px.
       - Tracking indicator: Anchor center, size 32x32.
    5. Add child nodes (Labels, Buttons) per UI-SPEC copywriting:
       - Insect info panel: Heading "Insect Info", Label "Species: [placeholder]", Label "Status: [placeholder]", Button "Track Insect".
       - Observation hint panel: Label "Observation Hint: [placeholder]".
       - Camera controls panel: Buttons "Track Insect", "Free Camera", "Zoom In", "Zoom Out", "Pan Left", "Pan Right", "Rotate".
       - Game status panel: Label "Time: 00:00", Label "Observed: 0".
       - Tracking indicator: a simple crosshair or circle sprite.
    6. Use appropriate font sizes from theme (heading, body, label).
    7. Apply colors: background panels use secondary color (#2D2D2D), buttons use accent color (#4CAF50) for interactive elements.
    8. Apply spacing constants for margins and gaps between elements.
    9. Save each scene in the appropriate path.
  </action>
  <verify>
    <automated>test -f "res://ui/panels/insect_info_panel.tscn" &amp;&amp; test -f "res://ui/panels/observation_hint_panel.tscn" &amp;&amp; test -f "res://ui/panels/camera_controls_panel.tscn" &amp;&amp; test -f "res://ui/panels/game_status_panel.tscn" &amp;&amp; test -f "res://ui/tracking_indicator.tscn" &amp;&amp; echo "All UI scenes exist"</automated>
    - All five scene files exist in their respective paths.
    - Each scene's root node is a Control with correct anchor and margins.
    - Each panel contains the expected child nodes (Labels, Buttons) with correct text.
    - Theme override is set to `res://ui/theme.tres`.
    - Colors match UI-SPEC (background secondary, buttons accent).
  </verify>
  <done>Five UI scenes created, visually styled, and placed according to layout design.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI panels → game logic | UI displays information from simulation, but does not modify simulation state directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | UI buttons | accept | No authentication needed; buttons are local UI. |
| T-04-02 | Tampering | UI theme resource | mitigate | Store theme in version control; validate hash if needed. |
| T-04-03 | Repudiation | UI interactions | accept | No logging required for UI clicks. |
| T-04-04 | Information Disclosure | UI text | accept | UI text is non-sensitive. |
| T-04-05 | Denial of Service | UI rendering | accept | UI is lightweight; unlikely to cause performance issues. |
| T-04-06 | Elevation of Privilege | UI scripts | accept | No privilege escalation possible. |
</threat_model>

<verification>
- Open Godot Editor and verify all five scenes load without errors.
- Run the project (if possible) to see panels rendered in viewport.
- Check that theme colors are applied.
</verification>

<success_criteria>
- UI theme resource exists with correct colors and spacing.
- Five UI panel scenes exist with appropriate layout and styling.
- Panels are visually consistent with UI-SPEC design contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-enhanced-camera/04-01-SUMMARY.md`
</output>