---
phase: 02-basic-insect-implementation
plan: 03
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - scripts/insects/insect_animation.gd
  - animations/insects/insect_animations.tres
  - scenes/insects/insect_base.tscn
autonomous: true
requirements:
  - INSECT-04
user_setup: []

must_haves:
  truths:
    - "Insect has walk and idle animations playing on skeleton"
    - "Animation states are managed via state machine (idle, walk, turn, attack)"
    - "Custom script blends between animation states smoothly"
    - "Animation speed adjusts based on insect stats"
  artifacts:
    - path: "scripts/insects/insect_animation.gd"
      provides: "Animation controller with state machine and blending logic"
      contains: "enum InsectAnimationState, current_state, set_animation_state()"
    - path: "animations/insects/insect_animations.tres"
      provides: "AnimationLibrary resource with walk and idle animations"
      contains: "walk, idle animations keyed to skeleton bones"
    - path: "scenes/insects/insect_base.tscn"
      provides: "Updated scene with AnimationPlayer and AnimationTree"
      contains: "AnimationTree node with state machine, AnimationPlayer"
  key_links:
    - from: "scripts/insects/insect_animation.gd"
      to: "scenes/insects/insect_base.tscn"
      via: "script attached to AnimationTree or insect root"
      pattern: "func _ready(): setup_animation_tree()"
    - from: "scripts/insects/insect_animation.gd"
      to: "scripts/insects/insect.gd"
      via: "reference to insect stats"
      pattern: "@onready var insect: Node = get_parent()"
---

<objective>
Implement skeletal animations for insect movement (walk, idle, turn, attack) with state machine and custom script blending.

Purpose: Provide basic insect animations per D-05 (skeletal animations), D-07 (walk, idle, turn, attack states), and D-08 (custom script blending). This brings insects to life with visible movement, fulfilling INSECT-04.

Output: Working animation system with walk and idle animations playing on insect skeleton, state transitions, and integration with insect stats.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-basic-insect-implementation/02-CONTEXT.md
@.planning/phases/02-basic-insect-implementation/02-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/NATURAL_ENVIRONMENT.md
@.planning/research/OBSERVATION_GAMEPLAY.md

# Dependencies
@.planning/phases/02-basic-insect-implementation/02-01-PLAN.md

<interfaces>
<!-- From Plan 01 -->
From `scenes/insects/insect_base.tscn`:
- Skeleton3D with 6-10 bones
- MeshInstance3D skinned to skeleton

From `scripts/insects/insect.gd`:
```gdscript
@export var stats: InsectStats
signal animation_state_changed(new_state)
```

From CONTEXT.md decisions:
- D-05: Animation implementation: Skeletal animations
- D-06: Bone complexity: Moderate (6–10 bones per insect)
- D-07: Animation states needed: Walk, idle, turn, attack
- D-08: Animation state management: Custom script blending

From PATTERNS.md state machine pattern (ARCHITECTURE.md lines 169-178):
```gdscript
enum BattleState {PLAYER_TURN, ENEMY_TURN, ACTION_EXECUTION, BATTLE_END}
var current_state: BattleState = BattleState.PLAYER_TURN

func process_player_input(action):
    if current_state != BattleState.PLAYER_TURN:
        return
    current_state = BattleState.ACTION_EXECUTION
    execute_action(action)
```

From PATTERNS.md AnimationTree reference: Use Godot's AnimationTree with state machine for animation blending.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AnimationPlayer and AnimationTree to insect scene</name>
  <files>scenes/insects/insect_base.tscn, animations/insects/insect_animations.tres</files>
  <action>
1. Open `insect_base.tscn`.
2. Add `AnimationPlayer` node as child of root.
3. Create two basic animations:
   - `idle`: subtle breathing/wiggling motion (bones move slightly up/down).
   - `walk`: leg movement, body forward motion (translate root bone forward).
   Use keyframe animation on skeleton bones. Keep animations simple but recognizable.
4. Save animations to `animations/insects/insect_animations.tres` (AnimationLibrary).
5. Add `AnimationTree` node, set `anim_player` to the AnimationPlayer.
6. Configure AnimationTree: create state machine with states `idle` and `walk`. Add transitions between them.
7. Set `AnimationTree.active = true`.

Goal: Scene has playable animations via AnimationTree state machine.

Reference Godot documentation for skeletal animation and AnimationTree.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>AnimationPlayer has idle and walk animations; AnimationTree state machine configured; animations play when scene runs.</done>
</task>

<task type="auto">
  <name>Task 2: Create insect animation controller script</name>
<files>scripts/insects/insect_animation.gd</files>
  <action>
Create `scripts/insects/insect_animation.gd` that manages animation states and blending.

Script should:
1. Extend `Node` (attached to AnimationTree or insect root).
2. Define `enum InsectAnimationState {IDLE, WALK, TURN, ATTACK}`.
3. Track `current_state: InsectAnimationState`.
4. Provide `set_animation_state(new_state: InsectAnimationState)` that:
   - Validates transition (e.g., can't go from idle to attack if attack disabled).
   - Sets AnimationTree state machine parameter.
   - Emits signal `animation_state_changed`.
5. Implement custom blending logic per D-08: when transitioning between states, cross‑fade animations over 0.2 seconds using AnimationTree blend times or custom tween.
6. Connect to insect stats: adjust animation speed based on `stats.walk_speed` for walk animation.

Follow state machine pattern from PATTERNS.md. Use signals for loose coupling.

Add to insect scene: attach script to AnimationTree or root.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Script exists with state enum and set_animation_state method; transitions trigger animation changes.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate animation controller and test states</name>
<files>scenes/insects/insect_base.tscn, scripts/insects/insect.gd</files>
  <action>
1. Attach `insect_animation.gd` to AnimationTree node in insect scene.
2. Modify `insect.gd` to reference animation controller and set initial state (IDLE).
3. Add temporary debug input: press key to toggle between IDLE and WALK for testing.
4. Ensure animation speed scales with `stats.walk_speed` (walk animation playback speed).
5. Test turn animation (rotate skeleton root bone) and attack animation (optional forward lunge).
6. Verify custom blending works: transition between states has smooth cross‑fade.

Integration points:
- Insect stats → animation speed.
- Insect script → animation state changes (future: based on movement).
- Signals between components.

This implements D-08 (custom script blending) and D-07 (states).
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Insect scene plays idle animation by default; debug input switches to walk; animations blend smoothly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Animation state transitions | Internal logic only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Denial of Service | Animation processing | accept | Animation complexity low; no external input. |
| T-02-06 | Tampering | Animation state | accept | State changes controlled by internal script only. |
</threat_model>

<verification>
- Run scene; insect plays idle animation.
- Trigger debug input; switches to walk animation with smooth blend.
- Check AnimationTree state machine parameters update.
</verification>

<success_criteria>
1. Insect has idle and walk skeletal animations.
2. Animation state machine manages transitions between states.
3. Custom blending provides smooth cross‑fade between animations.
4. Animation speed adjusts based on insect stats.
</success_criteria>

<output>
After completion, create `.planning/phases/02-basic-insect-implementation/02-03-SUMMARY.md`
</output>