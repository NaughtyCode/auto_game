---
phase: 04-ui-enhanced-camera
plan: 07
type: execute
wave: 3
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - res://ui/panels/insect_info_panel.tscn
  - res://ui/panels/insect_info_panel.gd
  - res://ui/panels/observation_hint_panel.tscn
  - res://ui/panels/observation_hint_panel.gd
  - res://ui/panels/game_status_panel.tscn
  - res://ui/tracking_indicator.tscn
  - res://ui/panels/camera_controls_panel.tscn
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - "Insect info panel slides in from right when insect selected, slides out when deselected"
    - "Observation hint panel fades in when hint appears, fades out after duration"
    - "Tracking indicator pulses softly when tracking mode is active"
    - "UI buttons have visual feedback on hover (scale change or color shift)"
  artifacts:
    - path: "res://ui/panels/insect_info_panel.gd"
      provides: "Slide animation logic for insect info panel"
      contains: "Tween slide_tween, function slide_in, function slide_out"
    - path: "res://ui/panels/observation_hint_panel.gd"
      provides: "Fade animation logic for hint panel"
      contains: "Tween fade_tween, function fade_in, function fade_out"
    - path: "res://ui/tracking_indicator.tscn"
      provides: "Pulsing animation for tracking indicator"
      contains: "Tween or AnimationPlayer with pulse animation"
    - path: "res://ui/panels/camera_controls_panel.tscn"
      provides: "Button hover state visual feedback"
      contains: "Button mouse_entered/mouse_exited signals connected"
  key_links:
    - from: "res://ui/panels/insect_info_panel.gd"
      to: "res://scripts/signal_bus.gd"
      via: "signal connection"
      pattern: "SignalBus.insect_selected.connect"
    - from: "res://ui/panels/insect_info_panel.gd"
      to: "res://ui/panels/insect_info_panel.tscn"
      via: "Tween animation on position"
      pattern: "tween_property(panel, 'position')"
    - from: "res://ui/panels/observation_hint_panel.gd"
      to: "res://scripts/hint_manager.gd"
      via: "function call"
      pattern: "HintPanel.display_hint"
    - from: "res://ui/tracking_indicator.tscn"
      to: "res://scripts/signal_bus.gd"
      via: "signal connection"
      pattern: "SignalBus.tracking_mode_toggled.connect"
---

<objective>
Add subtle animations and visual feedback to UI elements to enhance player experience and polish.

Purpose: Make UI feel more responsive and engaging with smooth transitions, visual feedback, and polished effects.

Output: Animated insect info panel (slide), observation hint panel (fade), tracking indicator (pulse), and button hover states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-enhanced-camera/04-UI-SPEC.md
@.planning/phases/04-ui-enhanced-camera/04-01-PLAN.md
@.planning/phases/04-ui-enhanced-camera/04-02-PLAN.md

**Existing UI scenes:**
- Insect info panel: shows insect species and status, appears top-right.
- Observation hint panel: displays hints bottom-right.
- Game status panel: top-left.
- Tracking indicator: center-screen reticle.
- Camera controls panel: bottom-center buttons.

**Godot animation tools:** Use Tween nodes or AnimationPlayer for simple animations.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add slide animation to insect info panel</name>
  <files>
    res://ui/panels/insect_info_panel.tscn
    res://ui/panels/insect_info_panel.gd
  </files>
  <read_first>
    - res://ui/panels/insect_info_panel.tscn (current scene structure)
    - res://ui/panels/insect_info_panel.gd (existing script from Plan 02)
    - Godot documentation on Tween and Control node properties.
  </read_first>
  <action>
    1. Open `insect_info_panel.tscn` in Godot Editor.
    2. Ensure the root Control node has a `modulate` property (for fade) and `position` property (for slide).
    3. In `insect_info_panel.gd`, add a Tween variable:
       ```gdscript
       var slide_tween: Tween
       ```
    4. In `_ready`, create a Tween node as child: `slide_tween = create_tween()`.
    5. Implement function `slide_in()`:
       - Set panel position off-screen right (e.g., `position.x = 400`).
       - Use `slide_tween.tween_property(self, "position:x", target_x, 0.3)` where target_x is the original anchored position (likely 0 relative to anchor).
       - Also tween `modulate.a` from 0 to 1 for fade-in.
    6. Implement function `slide_out()`:
       - Tween position.x off-screen right and modulate.a to 0.
    7. Modify `_on_insect_selected` to call `slide_in()` after updating labels.
    8. Modify `_on_insect_deselected` to call `slide_out()`.
    9. Ensure panel starts hidden (modulate.a = 0) or off-screen.
    10. Save script and scene.
  </action>
  <verify>
    <automated>test -f "res://ui/panels/insect_info_panel.gd" && grep -q "slide_tween" "res://ui/panels/insect_info_panel.gd" && echo "Insect info panel script contains slide_tween"</automated>
    - `insect_info_panel.gd` contains `slide_tween` variable and `slide_in`/`slide_out` functions.
    - Panel slides in from right when insect selected (visible movement).
    - Panel slides out when deselected.
    - Animation uses Tween with duration 0.3 seconds.
  </verify>
  <done>Insect info panel has smooth slide animation on selection/deselection.</done>
</task>

<task type="auto">
  <name>Task 2: Add fade animation to observation hint panel</name>
  <files>
    res://ui/panels/observation_hint_panel.tscn
    res://ui/panels/observation_hint_panel.gd
  </files>
  <read_first>
    - res://ui/panels/observation_hint_panel.tscn (current scene)
    - res://ui/panels/observation_hint_panel.gd (existing script from Plan 06)
    - Godot Tween documentation.
  </read_first>
  <action>
    1. Open `observation_hint_panel.tscn`.
    2. Ensure root Control has `modulate` property.
    3. In `observation_hint_panel.gd`, add Tween variable `fade_tween`.
    4. In `_ready`, create tween: `fade_tween = create_tween()`.
    5. Modify `display_hint(text)` function:
       - Set label text.
       - Set `modulate.a = 0` (invisible).
       - Call `fade_in()`.
    6. Implement `fade_in()`:
       - `fade_tween.tween_property(self, "modulate:a", 1.0, 0.5)`.
    7. Implement `hide_hint()` (if not exists):
       - `fade_tween.tween_property(self, "modulate:a", 0.0, 0.5)`.
       - After tween completed, hide panel (optional).
    8. Ensure hint panel starts with modulate.a = 0 (hidden).
    9. Save script and scene.
  </action>
  <verify>
    <automated>test -f "res://ui/panels/observation_hint_panel.gd" && grep -q "fade_tween" "res://ui/panels/observation_hint_panel.gd" && echo "Observation hint panel script contains fade_tween"</automated>
    - `observation_hint_panel.gd` contains fade_tween and fade_in/hide_hint functions.
    - Panel fades in when hint displayed (opacity 0 → 1 over 0.5 seconds).
    - Panel fades out when hint hidden.
    - Animation uses Tween.
  </verify>
  <done>Observation hint panel fades in and out smoothly.</done>
</task>

<task type="auto">
  <name>Task 3: Add pulsing animation to tracking indicator and button hover feedback</name>
  <files>
    res://ui/tracking_indicator.tscn
    res://ui/panels/camera_controls_panel.tscn
  </files>
  <read_first>
    - res://ui/tracking_indicator.tscn (current scene)
    - res://ui/panels/camera_controls_panel.tscn (current scene)
    - Godot AnimationPlayer or Tween for looping pulse.
  </read_first>
  <action>
    1. **Tracking indicator pulse:**
       - Open `tracking_indicator.tscn`.
       - Add an AnimationPlayer node as child of root.
       - Create a new animation "pulse".
       - Add keyframes for `scale` property: start at (1,1), scale to (1.2,1.2) at 0.5 sec, back to (1,1) at 1 sec.
       - Set animation loop (ping-pong or linear loop).
       - In script (or via signal), start animation when tracking mode active.
       - Connect to `SignalBus.tracking_mode_toggled`: if enabled, play "pulse"; else stop.
    2. **Button hover feedback:**
       - Open `camera_controls_panel.tscn`.
       - For each Button node, connect `mouse_entered` and `mouse_exited` signals.
       - In connected function, tween the button's `scale` property to (1.1,1.1) on hover, back to (1,1) on exit.
       - Alternatively, change `modulate` color slightly.
    3. Save both scenes.
  </action>
  <verify>
    <automated>test -f "res://ui/tracking_indicator.tscn" && grep -q "AnimationPlayer" "res://ui/tracking_indicator.tscn" 2>/dev/null && echo "Tracking indicator has AnimationPlayer" || echo "Tracking indicator may need AnimationPlayer"</automated>
    - `tracking_indicator.tscn` contains AnimationPlayer with "pulse" animation.
    - Pulse animation loops while tracking active.
    - Camera control buttons scale up slightly when mouse hovers over them.
    - Button hover effect is smooth (tween).
  </verify>
  <done>Tracking indicator pulses and buttons have hover feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI animations → game state | Animations are purely visual; no effect on simulation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-37 | Spoofing | Animation triggers | accept | Triggers based on signals from trusted systems. |
| T-04-38 | Tampering | Animation parameters | accept | Parameters are local; no external modification. |
| T-04-39 | Repudiation | Animation playback | accept | No need to log. |
| T-04-40 | Information Disclosure | Animation timing | accept | Non-sensitive. |
| T-04-41 | Denial of Service | Animation performance | accept | Simple tweens; negligible performance impact. |
| T-04-42 | Elevation of Privilege | Animation scripts | accept | No privileged operations. |
</threat_model>

<verification>
- Run project, select insect and verify insect info panel slides in.
- Deselect, verify slide out.
- Trigger observation hint, verify fade in.
- Enable tracking mode, verify tracking indicator pulses.
- Hover over camera control buttons, verify scale change.
</verification>

<success_criteria>
- All four animation enhancements are present and functional.
- Animations are smooth and non-intrusive.
- No performance degradation observed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-enhanced-camera/04-07-SUMMARY.md`
</output>