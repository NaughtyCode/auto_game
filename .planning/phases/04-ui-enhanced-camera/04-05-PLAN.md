---
phase: 04-ui-enhanced-camera
plan: 05
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - res://scripts/game_manager.gd
  - res://ui/panels/game_status_panel.tscn
  - res://ui/panels/game_status_panel.gd
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "Game status panel displays elapsed game time"
    - "Game status panel displays observation count"
    - "Observation count increments when new insect is selected"
  artifacts:
    - path: "res://scripts/game_manager.gd"
      provides: "Game state tracking"
      contains: "elapsed_time float, observation_count int, function increment_observation"
    - path: "res://ui/panels/game_status_panel.gd"
      provides: "Game status UI logic"
      contains: "function _process updating time label, function update_observation_label"
    - path: "res://ui/panels/game_status_panel.tscn"
      provides: "Updated scene with script attached"
      contains: "script reference to game_status_panel.gd"
  key_links:
    - from: "res://ui/panels/game_status_panel.gd"
      to: "res://scripts/game_manager.gd"
      via: "property access"
      pattern: "GameManager.elapsed_time"
    - from: "res://scripts/game_manager.gd"
      to: "res://scripts/signal_bus.gd"
      via: "signal connection"
      pattern: "SignalBus.insect_selected.connect"
---

<objective>
Implement game status UI panel that displays elapsed game time and insect observation count.

Purpose: Provide player with basic progress tracking and time awareness during observation.

Output: GameManager singleton tracking time and observations, and game status panel that updates accordingly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-enhanced-camera/04-UI-SPEC.md
@.planning/phases/04-ui-enhanced-camera/04-01-PLAN.md

**Assumptions:**
- SignalBus exists with `insect_selected` signal.
- Game runs in real-time; elapsed time is real seconds.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GameManager autoload</name>
  <files>res://scripts/game_manager.gd</files>
<read_first>
    - Existing autoloads (SignalBus) to follow naming pattern.
  </read_first>
  <action>
    1. Ensure directory `res://scripts/` exists.
    2. Create script `game_manager.gd` extending `Node`.
    3. Define variables:
       ```gdscript
       var elapsed_time: float = 0.0
       var observation_count: int = 0
       var observed_insects: Array = []  # store IDs or node references
       ```
    4. In `_process(delta)`:
       - `elapsed_time += delta`
    5. Define function `increment_observation(insect_node)`:
       - If insect_node not in observed_insects:
         - `observation_count += 1`
         - `observed_insects.append(insect_node)`
    6. In `_ready`:
       - Connect `SignalBus.insect_selected` to `increment_observation`.
    7. Save script.
    8. Register as autoload in Project Settings > AutoLoad with name `GameManager`.
       - Provide instruction if needed.
  </action>
  <verify>
    <automated>test -f "res://scripts/game_manager.gd" &amp;&amp; grep -q "elapsed_time" "res://scripts/game_manager.gd" &amp;&amp; echo "GameManager script exists with time tracking"</automated>
    - File `res://scripts/game_manager.gd` exists with elapsed_time and observation_count.
    - `_process` updates elapsed_time.
    - `increment_observation` increments count only for new insects.
    - Autoload `GameManager` appears in Project Settings.
  </verify>
  <done>GameManager tracks time and observation count.</done>
</task>

<task type="auto">
  <name>Task 2: Connect game status panel to GameManager</name>
  <files>
    res://ui/panels/game_status_panel.tscn
    res://ui/panels/game_status_panel.gd
  </files>
  <read_first>
    - res://ui/panels/game_status_panel.tscn (created in Plan 01)
    - res://scripts/game_manager.gd
  </read_first>
  <action>
    1. Open `game_status_panel.tscn` in Godot Editor.
    2. Attach a new script `game_status_panel.gd` to the root Control node.
    3. Define references to time and observation labels (use `$TimeLabel`, `$ObservationLabel`).
    4. In `_ready`:
       - Optionally connect to GameManager signals if we add them (not needed; we'll poll).
    5. In `_process(delta)`:
       - Update time label: format elapsed_time as minutes:seconds (`var minutes = int(GameManager.elapsed_time) / 60`, etc.)
       - Update observation label: `$ObservationLabel.text = "Observed: " + str(GameManager.observation_count)`
    6. Save script and scene.
  </action>
  <verify>
    <automated>test -f "res://ui/panels/game_status_panel.gd" &amp;&amp; grep -q "_process" "res://ui/panels/game_status_panel.gd" &amp;&amp; echo "Game status panel script exists with update logic"</automated>
    - Script `game_status_panel.gd` exists and is attached to scene.
    - Time label updates continuously showing formatted time.
    - Observation label shows current observation count.
    - Panel updates when observation count changes (via polling).
  </verify>
  <done>Game status panel displays live game time and observation count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameManager → observation count | Count increments based on selection; no validation needed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-25 | Spoofing | Observation count | accept | Count increments on selection; player cannot spoof. |
| T-04-26 | Tampering | GameManager variables | accept | Variables are internal; no external modification. |
| T-04-27 | Repudiation | Observation events | accept | No need to log. |
| T-04-28 | Information Disclosure | Game time | accept | Non-sensitive. |
| T-04-29 | Denial of Service | Time accumulation | accept | Float increment negligible. |
| T-04-30 | Elevation of Privilege | GameManager | accept | No privileged operations. |
</threat_model>

<verification>
- Run project, verify game status panel shows time increasing.
- Select an insect, verify observation count increments.
- Select same insect again, verify count does not increment.
</verification>

<success_criteria>
- Game status panel displays formatted elapsed time.
- Observation count increments on first selection of each insect.
- Panel updates in real-time.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-enhanced-camera/04-05-SUMMARY.md`
</output>