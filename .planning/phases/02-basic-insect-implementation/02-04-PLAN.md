---
phase: 02-basic-insect-implementation
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - scripts/managers/insect_manager.gd
  - scenes/insects/insect_base.tscn
  - scenes/main.tscn
autonomous: true
requirements:
  - INSECT-01
  - ENV-02
user_setup: []

must_haves:
  truths:
    - "Developer can spawn insects of specific species via script"
    - "5–10 insects per species exist in environment (15–30 total)"
    - "Insects are positioned on terrain surfaces (not floating)"
    - "Insect manager tracks insect count and provides access to insects"
  artifacts:
    - path: "scripts/managers/insect_manager.gd"
      provides: "Central manager for insect spawning, tracking, and removal"
      contains: "func spawn_insect(species: String, position: Vector3), get_insect_count()"
    - path: "scenes/main.tscn"
      provides: "Main game scene with InsectManager node and spawned insects"
      contains: "InsectManager node, multiple insect instances as children"
    - path: "scenes/insects/insect_base.tscn"
      provides: "Updated insect scene with terrain placement logic"
      contains: "func place_on_terrain(terrain_node: Node3D)"
  key_links:
    - from: "scripts/managers/insect_manager.gd"
      to: "scenes/insects/insect_base.tscn"
      via: "preload and instantiate"
      pattern: "var insect_scene = preload('res://scenes/insects/insect_base.tscn')"
    - from: "scenes/main.tscn"
      to: "scripts/managers/insect_manager.gd"
      via: "InsectManager node"
      pattern: "onready var insect_manager = $InsectManager"
    - from: "scenes/insects/insect_base.tscn"
      to: "terrain (from Phase 1)"
      via: "placement function"
      pattern: "global_position = terrain.get_height_at(position)"
---

<objective>
Create insect manager to spawn and manage insect populations, positioning them on terrain surfaces.

Purpose: Implement D-11 (population density 5–10 insects per species) and D-12 (5–10 insects per species). Enable observing multiple insect species in environment (INSECT‑01) interacting with terrain (ENV‑02). The manager will be the central point for insect lifecycle.

Output: InsectManager script, integration into main scene, 15–30 insects placed on terrain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-basic-insect-implementation/02-CONTEXT.md
@.planning/phases/02-basic-insect-implementation/02-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/NATURAL_ENVIRONMENT.md

# Dependencies
@.planning/phases/02-basic-insect-implementation/02-01-PLAN.md
@.planning/phases/02-basic-insect-implementation/02-02-PLAN.md
@.planning/phases/02-basic-insect-implementation/02-03-PLAN.md

# Phase 1 assumed environment
@.planning/phases/01-environment-camera-setup/01-CONTEXT.md

<interfaces>
<!-- From Plan 01 -->
From `scenes/insects/insect_base.tscn`:
- CharacterBody3D root with `insect.gd` script
- `@export var stats: InsectStats`

From `resources/insect_stats_*.tres`:
- Three species resources

From CONTEXT.md decisions:
- D-09: Species: cricket, beetle, spider
- D-11: Insect size: 0.1 m
- D-12: Population density: 5–10 insects per species

From PATTERNS.md manager pattern (ARCHITECTURE.md lines 118-131):
```gdscript
signal action_selected(action, skill)
signal turn_ended

func _on_action_selected(action: String, skill: Skill) -> void:
    if validate_ap(current_battler, action):
        current_action = action
        current_skill = skill
        emit_signal("action_selected", action, skill)
```

From PATTERNS.md InsectManager responsibility: CRUD operations.

From Phase 1 terrain (assumed):
- Terrain node exists in main scene
- Terrain has collision and surface
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create insect manager script with spawning logic</name>
<files>scripts/managers/insect_manager.gd</files>
  <action>
Create `scripts/managers/insect_manager.gd` extending `Node`.

Key features:
1. Signals: `insect_spawned(insect_node)`, `insect_removed(insect_node)`, `insect_count_changed(count)`.
2. Properties: `var insects: Array = []` (track insect nodes).
3. Methods:
   - `spawn_insect(species: String, position: Vector3) -> Node3D`: 
     * Load appropriate stats resource based on species.
     * Instantiate `insect_base.tscn`.
     * Assign stats resource.
     * Add as child to manager (or designated parent).
     * Add to insects array, emit signals.
   - `remove_insect(insect_node: Node3D)`: remove from array, queue_free.
   - `get_insect_count() -> int`: return insects.size().
   - `get_insects_by_species(species: String) -> Array`: filter.
4. Helper: `spawn_random_insect()` picks random species and random position.

Follow signal pattern from PATTERNS.md. Use preload for scene.

Integrate with terrain later in Task 2.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Script exists with spawn/remove methods; signals defined; can instantiate insect scene.</done>
</task>

<task type="auto">
  <name>Task 2: Add terrain placement logic to insect scene</name>
<files>scenes/insects/insect_base.tscn, scripts/insects/insect.gd</files>
  <action>
Enhance `insect.gd` to place insect on terrain surface.

Add method `place_on_terrain(terrain_node: Node3D, world_position: Vector2)`:
1. Use `terrain_node` to get height at `world_position` (assuming terrain has function `get_height_at(x, z)`).
2. If terrain doesn't expose height, use raycast downward from above terrain to find collision.
3. Set insect `global_position` to `Vector3(world_position.x, height, world_position.y)`.
4. Ensure insect is oriented upright (y‑up).

Modify insect manager's `spawn_insect` to call `insect.place_on_terrain(terrain, position)` after instantiation.

For Phase 1 terrain, assume flat terrain at y=0, so height = 0. Implement raycast fallback for robustness.

This implements ENV‑02 (insects interacting with terrain features).
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Insect has place_on_terrain method; insect manager uses it; insects spawn at terrain height.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate manager into main scene and spawn initial population</name>
<files>scenes/main.tscn</files>
  <action>
1. Open main scene (created in Phase 1).
2. Add `InsectManager` node as child of root (or appropriate parent).
3. Attach `insect_manager.gd` script.
4. In manager `_ready()`, spawn initial population:
   - 5–10 crickets at random positions within terrain bounds.
   - 5–10 beetles at random positions.
   - 5–10 spiders at random positions.
   Use `spawn_insect` with species string and random Vector2 within terrain bounds.
5. Ensure insects are children of a dedicated node (e.g., `Insects` folder) for organization.
6. Verify total insect count 15–30.

Terrain bounds: use Phase 1 terrain size (assume 10×10 tiles of 0.5 m = 5 m square). Randomize within that.

Test: run scene, see insects placed on terrain, not floating.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test scaffolding first</automated>
  </verify>
  <done>Main scene contains InsectManager; 15–30 insects visible on terrain; species mix present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | All spawning internal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Denial of Service | Unlimited spawning | accept | Spawning limited to initial population; no external input. |
| T-02-08 | Information Disclosure | Insect count | accept | Count is not sensitive data. |
</threat_model>

<verification>
- Run main scene; count insects in viewport (should be 15–30).
- Check insect positions are on terrain surface (y ≈ 0).
- Open scene tree, verify InsectManager node and insect children.
</verification>

<success_criteria>
1. Insect manager spawns insects of specified species.
2. 15–30 insects exist in environment (5–10 per species).
3. Insects are positioned on terrain surfaces, not floating.
4. Insect manager tracks count and provides access to insects.
</success_criteria>

<output>
After completion, create `.planning/phases/02-basic-insect-implementation/02-04-SUMMARY.md`
</output>