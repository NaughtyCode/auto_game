---
phase: 04-ui-enhanced-camera
plan: 04
type: execute
wave: 4
depends_on:
  - 04-01-PLAN.md
  - 04-03-PLAN.md
files_modified:
  - res://ui/panels/camera_controls_panel.tscn
  - res://ui/panels/camera_controls_panel.gd
  - res://scripts/camera_free_controller.gd
autonomous: true
requirements:
  - UI-03
must_haves:
  truths:
    - "Player can click UI buttons to toggle tracking mode"
    - "Player can click UI buttons to zoom in/out, pan left/right, rotate camera"
    - "Buttons visually reflect active state (e.g., tracking button highlighted when tracking)"
  artifacts:
    - path: "res://ui/panels/camera_controls_panel.gd"
      provides: "Camera controls UI logic"
      contains: "function _ready connecting button signals, function _on_track_toggled"
    - path: "res://scripts/camera_free_controller.gd"
      provides: "Central controller for free camera actions"
      contains: "functions zoom_in, zoom_out, pan_left, pan_right, rotate"
    - path: "res://ui/panels/camera_controls_panel.tscn"
      provides: "Updated scene with button signal connections"
      contains: "signal connections to script functions"
  key_links:
    - from: "res://ui/panels/camera_controls_panel.gd"
      to: "res://scripts/camera_free_controller.gd"
      via: "function calls"
      pattern: "CameraFreeController.zoom_in()"
    - from: "res://ui/panels/camera_controls_panel.gd"
      to: "res://scripts/signal_bus.gd"
      via: "signal emission"
      pattern: "SignalBus.tracking_mode_toggled.emit"
---

<objective>
Implement camera controls UI panel with functional buttons for tracking toggle, zoom, pan, and rotate.

Purpose: Provide on-screen controls for camera manipulation, enhancing player observation experience.

Output: Camera controls panel wired to camera systems, with buttons that affect camera behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-enhanced-camera/04-UI-SPEC.md
@.planning/phases/04-ui-enhanced-camera/04-01-PLAN.md
@.planning/phases/04-ui-enhanced-camera/04-03-PLAN.md

**Assumptions:**
- Phase 1 provides a CameraFree script with functions for zoom, pan, rotate. We'll create a placeholder `CameraFreeController` that can be replaced later.
- CameraManager exists (from Plan 03) and handles tracking mode.
- SignalBus has `tracking_mode_toggled` signal.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CameraFreeController placeholder</name>
  <files>res://scripts/camera_free_controller.gd</files>
  <read_first>
    - Existing camera scripts (if any) to avoid conflict.
  </read_first>
  <action>
    1. Ensure directory `res://scripts/` exists.
    2. Create script `camera_free_controller.gd` extending `Node`.
    3. Define functions that will be called by UI buttons:
       ```gdscript
       func zoom_in():
           print("Zoom in")
           # Phase 1 will implement actual zoom
       
       func zoom_out():
           print("Zoom out")
       
       func pan_left():
           print("Pan left")
       
       func pan_right():
           print("Pan right")
       
       func rotate():
           print("Rotate")
       ```
    4. Register as autoload in Project Settings > AutoLoad with name `CameraFreeController`.
       - If cannot edit via script, provide instruction: "Add `res://scripts/camera_free_controller.gd` as autoload `CameraFreeController`."
    5. Save script.
  </action>
  <verify>
    <automated>test -f "res://scripts/camera_free_controller.gd" &amp;&amp; grep -q "zoom_in" "res://scripts/camera_free_controller.gd" &amp;&amp; echo "CameraFreeController script exists with functions"</automated>
    - File `res://scripts/camera_free_controller.gd` exists with five functions.
    - Autoload `CameraFreeController` appears in Project Settings.
  </verify>
  <done>Placeholder controller available for UI buttons to call.</done>
</task>

<task type="auto">
  <name>Task 2: Add script to camera controls panel</name>
  <files>
    res://ui/panels/camera_controls_panel.tscn
    res://ui/panels/camera_controls_panel.gd
  </files>
  <read_first>
    - res://ui/panels/camera_controls_panel.tscn (created in Plan 01)
    - res://scripts/camera_free_controller.gd
    - res://scripts/signal_bus.gd
  </read_first>
  <action>
    1. Open `camera_controls_panel.tscn` in Godot Editor.
    2. Attach a new script `camera_controls_panel.gd` to the root Control node.
    3. Define references to buttons (use `$ButtonName` or `@onready`).
    4. In `_ready`:
       - Connect each button's `pressed` signal to a corresponding function.
       - For "Track Insect" button, connect to `_on_track_button_pressed`.
       - For "Free Camera" button, connect to `_on_free_camera_button_pressed`.
       - For zoom/pan/rotate buttons, connect to functions that call CameraFreeController.
    5. Implement button handlers:
       ```gdscript
       func _on_track_button_pressed():
           SignalBus.tracking_mode_toggled.emit(true)
           # Update button visual state (e.g., highlight)
       
       func _on_free_camera_button_pressed():
           SignalBus.tracking_mode_toggled.emit(false)
       
       func _on_zoom_in_pressed():
           CameraFreeController.zoom_in()
       
       # ... similar for others
       ```
    6. Optionally update button appearance based on tracking mode (e.g., change color). Connect to `SignalBus.tracking_mode_toggled` to update.
    7. Save script and scene.
  </action>
  <verify>
    <automated>test -f "res://ui/panels/camera_controls_panel.gd" &amp;&amp; grep -q "track_button_pressed" "res://ui/panels/camera_controls_panel.gd" &amp;&amp; echo "Camera controls panel script exists with button handlers"</automated>
    - Script `camera_controls_panel.gd` exists and is attached to scene.
    - Buttons are connected to script functions.
    - Pressing "Track Insect" emits `tracking_mode_toggled(true)`.
    - Pressing "Free Camera" emits `tracking_mode_toggled(false)`.
    - Pressing zoom/pan/rotate buttons calls corresponding placeholder functions.
  </verify>
  <done>Camera controls panel script handles button presses and emits appropriate signals.</done>
</task>

<task type="auto">
  <name>Task 3: Connect CameraManager to tracking mode toggle</name>
  <files>res://scripts/camera_manager.gd</files>
  <read_first>
    - res://scripts/camera_manager.gd (from Plan 03)
    - res://scripts/signal_bus.gd
  </read_first>
  <action>
    1. Open `camera_manager.gd`.
    2. In `_ready`, connect `SignalBus.tracking_mode_toggled` to a function `_on_tracking_mode_toggled`.
    3. Implement `_on_tracking_mode_toggled(enabled: bool)`:
       - Call `set_tracking_mode(enabled)` (already defined).
    4. Save script.
  </action>
  <verify>
    <automated>grep -q "tracking_mode_toggled" "res://scripts/camera_manager.gd" 2>/dev/null &amp;&amp; echo "CameraManager connects to tracking signal" || echo "CameraManager may need update"</automated>
    - CameraManager connects to `tracking_mode_toggled` signal.
    - When signal emitted with `true`, `tracking_enabled` becomes true and camera follow target is set (if insect selected).
    - When signal emitted with `false`, tracking disabled and camera follow target cleared.
  </verify>
  <done>CameraManager responds to UI tracking toggle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI buttons → camera actions | UI triggers camera movements; no validation needed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Spoofing | Button presses | accept | Local UI, cannot be spoofed. |
| T-04-20 | Tampering | CameraFreeController | accept | Placeholder; Phase 1 will implement secure functions. |
| T-04-21 | Repudiation | Camera actions | accept | No need to log. |
| T-04-22 | Information Disclosure | Camera state | accept | Non-sensitive. |
| T-04-23 | Denial of Service | Rapid button clicks | accept | UI debouncing not needed for now. |
| T-04-24 | Elevation of Privilege | Camera control | accept | No privilege escalation. |
</threat_model>

<verification>
- Run project, click camera control buttons and verify console prints.
- Click Track Insect button, verify CameraManager enables tracking mode.
- Click Free Camera button, verify tracking mode disabled.
</verification>

<success_criteria>
- All camera control buttons trigger appropriate functions or signals.
- Tracking mode toggle works via UI.
- CameraFreeController autoload exists.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-enhanced-camera/04-04-SUMMARY.md`
</output>