---
phase: 04-ui-enhanced-camera
plan: 08
type: execute
wave: 4
depends_on:
  - 04-02-PLAN.md
  - 04-07-PLAN.md
files_modified:
  - res://ui/panels/insect_info_panel.tscn
  - res://ui/panels/insect_info_panel.gd
  - res://scripts/signal_bus.gd
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Insect info panel displays hunger level (0–100) of selected insect"
    - "Insect info panel displays energy level (0–100) of selected insect"
    - "Insect info panel displays current behavior (e.g., 'idle', 'walking', 'eating')"
    - "Hunger and energy levels update periodically while insect is selected"
  artifacts:
    - path: "res://ui/panels/insect_info_panel.tscn"
      provides: "Additional UI labels for hunger, energy, behavior"
      contains: "Label 'Hunger:', Label 'Energy:', Label 'Behavior:' with placeholder values"
    - path: "res://ui/panels/insect_info_panel.gd"
      provides: "State update logic for simulation variables"
      contains: "function update_state, Timer for periodic refresh, connection to simulation signals"
    - path: "res://scripts/signal_bus.gd"
      provides: "Signal for insect state updates"
      contains: "signal insect_state_updated(insect_node: Node)"
  key_links:
    - from: "res://ui/panels/insect_info_panel.gd"
      to: "res://scripts/signal_bus.gd"
      via: "signal connection"
      pattern: "SignalBus.insect_state_updated.connect"
    - from: "res://scripts/signal_bus.gd"
      to: "Phase 3 simulation scripts"
      via: "signal emission"
      pattern: "SignalBus.insect_state_updated.emit"
    - from: "res://ui/panels/insect_info_panel.gd"
      to: "selected insect node"
      via: "property access"
      pattern: "insect_node.hunger"
---

<objective>
Enhance insect info panel to display simulation state variables (hunger, energy, current behavior) from Phase 3 simulation.

Purpose: Provide player with deeper insight into insect internal state, enriching observation gameplay.

Output: Updated insect info panel with additional labels, periodic state updates, and integration with simulation signals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-enhanced-camera/04-UI-SPEC.md
@.planning/phases/04-ui-enhanced-camera/04-02-PLAN.md

**Assumptions:**
- Phase 3 (Simulation Behaviors) will add `hunger`, `energy`, `current_behavior` properties to insect nodes.
- Phase 3 will emit `SignalBus.insect_state_updated` when insect state changes (optional).
- If signals not present, we will poll the insect node properties periodically.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend insect info panel UI with state labels</name>
  <files>res://ui/panels/insect_info_panel.tscn</files>
  <read_first>
    - res://ui/panels/insect_info_panel.tscn (current scene)
    - UI-SPEC.md for spacing and typography.
  </read_first>
  <action>
    1. Open `insect_info_panel.tscn` in Godot Editor.
    2. Add three new Label nodes as children of the panel (e.g., under a VBoxContainer if exists).
    3. Set label text:
       - "Hunger: 100"
       - "Energy: 100"
       - "Behavior: idle"
    4. Apply appropriate font sizes (Label small 14px).
    5. Arrange with consistent spacing (md = 16px between lines).
    6. Name nodes: `HungerLabel`, `EnergyLabel`, `BehaviorLabel`.
    7. Save scene.
  </action>
  <verify>
    <automated>test -f "res://ui/panels/insect_info_panel.tscn" && grep -q "HungerLabel" "res://ui/panels/insect_info_panel.tscn" 2>/dev/null && echo "Insect info panel scene contains HungerLabel"</automated>
    - Scene contains three new labels with correct names and placeholder text.
    - Labels are visible in the panel layout.
    - No overlapping with existing elements.
  </verify>
  <done>Insect info panel UI includes hunger, energy, behavior labels.</done>
</task>

<task type="auto">
  <name>Task 2: Add state update logic to insect info panel script</name>
  <files>res://ui/panels/insect_info_panel.gd</files>
  <read_first>
    - res://ui/panels/insect_info_panel.gd (existing script)
    - res://scripts/signal_bus.gd (to add new signal)
  </read_first>
  <action>
    1. Open `insect_info_panel.gd`.
    2. Add references to new labels:
       ```gdscript
       @onready var hunger_label: Label = $HungerLabel
       @onready var energy_label: Label = $EnergyLabel
       @onready var behavior_label: Label = $BehaviorLabel
       ```
    3. Add a Timer node for periodic polling (if signals not used). In `_ready`:
       ```gdscript
       var timer = Timer.new()
       add_child(timer)
       timer.wait_time = 0.5  # update twice per second
       timer.timeout.connect(_update_state)
       timer.start()
       ```
    4. Implement `_update_state()`:
       - If `current_insect` (stored from selection) is not null:
         - Read properties: `hunger = current_insect.hunger` (default to 100 if missing)
         - `energy = current_insect.energy`
         - `behavior = current_insect.current_behavior` (string)
         - Update labels: `hunger_label.text = "Hunger: " + str(hunger)`
         - etc.
    5. Modify `_on_insect_selected(insect_node)` to store `current_insect = insect_node` and call `_update_state()`.
    6. Modify `_on_insect_deselected` to clear `current_insect` and reset labels to placeholder.
    7. Save script.
  </action>
  <verify>
    <automated>test -f "res://ui/panels/insect_info_panel.gd" && grep -q "HungerLabel" "res://ui/panels/insect_info_panel.gd" && echo "Insect info panel script references new labels"</automated>
    - Script contains references to three new labels.
    - Timer exists and calls `_update_state` every 0.5 seconds.
    - `_update_state` reads properties from `current_insect` and updates labels.
    - Labels reset when insect deselected.
  </verify>
  <done>Insect info panel script updates state labels periodically.</done>
</task>

<task type="auto">
  <name>Task 3: Add insect state update signal to SignalBus (optional)</name>
  <files>res://scripts/signal_bus.gd</files>
  <read_first>
    - res://scripts/signal_bus.gd (current signals)
  </read_first>
  <action>
    1. Open `signal_bus.gd`.
    2. Add a new signal:
       ```gdscript
       signal insect_state_updated(insect_node: Node)
       ```
    3. Save script.
    4. (Optional) In insect info panel script, connect to this signal in `_ready` and call `_update_state` for the specific insect if it matches selected insect.
  </action>
  <verify>
    <automated>test -f "res://scripts/signal_bus.gd" && grep -q "insect_state_updated" "res://scripts/signal_bus.gd" && echo "SignalBus contains insect_state_updated signal"</automated>
    - SignalBus contains `insect_state_updated` signal.
    - No syntax errors.
  </verify>
  <done>SignalBus provides signal for simulation to notify state changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Insect state → UI display | UI reads simulation state; no modification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-43 | Spoofing | Insect state values | accept | Values come from simulation; cannot be spoofed. |
| T-04-44 | Tampering | UI labels | accept | Labels are read‑only display. |
| T-04-45 | Repudiation | State updates | accept | No need to log. |
| T-04-46 | Information Disclosure | Insect internal state | accept | Non-sensitive game data. |
| T-04-47 | Denial of Service | Timer frequency | accept | Low frequency (0.5s) negligible. |
| T-04-48 | Elevation of Privilege | Insect info script | accept | No privileged operations. |
</threat_model>

<verification>
- Run project, select an insect with `hunger`, `energy`, `current_behavior` properties (add test properties if missing).
- Verify hunger, energy, behavior labels show correct values.
- Deselect insect, verify labels reset to placeholder.
- Change insect properties in debug console, verify labels update within 0.5 seconds.
</verification>

<success_criteria>
- Insect info panel displays hunger, energy, behavior values for selected insect.
- Values update periodically while insect selected.
- Panel resets when insect deselected.
- SignalBus has insect_state_updated signal.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-enhanced-camera/04-08-SUMMARY.md`
</output>